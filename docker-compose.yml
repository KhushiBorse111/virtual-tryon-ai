version: '3'
services:
  app:
    image: my-image
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DB_HOST: db
  db:
    image: postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydb
